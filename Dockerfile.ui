# syntax=docker/dockerfile:1
# Streamlit UI image. Built per spec; not verified locally (no Docker daemon on the dev machine).
#   docker build -f Dockerfile.ui -t satsim-ui .
#   docker run --rm -p 8501:8501 satsim-ui   # then open http://localhost:8501
FROM python:3.12-slim AS base

COPY --from=ghcr.io/astral-sh/uv:latest /uv /uvx /bin/

ENV UV_LINK_MODE=copy \
    UV_COMPILE_BYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

# Install dependencies first (better layer caching), including the optional UI group.
COPY pyproject.toml uv.lock ./
RUN --mount=type=cache,target=/root/.cache/uv \
    uv sync --frozen --no-install-project --no-dev --group ui

# Copy the project and install it.
COPY . .
RUN --mount=type=cache,target=/root/.cache/uv \
    uv sync --frozen --no-dev --group ui

EXPOSE 8501
ENTRYPOINT ["uv", "run", "--no-dev", "--group", "ui", "streamlit", "run", "streamlit_app.py", \
            "--server.port=8501", "--server.address=0.0.0.0"]
